mixin rate(obj)
  ul.rate
    - for (let i = 0; i < 5; i++)
      if i < obj.num
        li.rate__star.rate__star_checked
          include ./starChecked.svg
      else
        li.rate__star
          include ./star.svg